# Archived

- **Closed**: 2026-07-19 18:49 UTC
- **Status**: Succeeded
- **Summary**: Added a macOS LaunchAgent installer script and wired it into `install.sh` so the Podman machine starts automatically at login, with an opt-out via `SANDBOX_SKIP_PODMAN_LAUNCHAGENT=1`.
- **Outcomes**: 3/3 tests passed.
- **Follow-up**: Merge PR #15 to promote the changes to `main`.
